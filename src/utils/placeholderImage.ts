/**
 * Fallback cover image for personal-blog articles that have no thumb/large
 * image in their frontmatter.
 */
export function getPlaceholderImage(): string {
    return '/thumbs/placeholder.webp';
}
